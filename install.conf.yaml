- defaults:
    link:
        relink: true

- clean: ['~']

- link:
    ~/.bash:
    ~/.bashrc:
    ~/.dotfiles: ''
    ~/.shell:
    ~/.vimrc:

- shell:
    - [git submodule update --init --recursive, Installing submodules]

